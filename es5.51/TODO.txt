- build Register class
- then Machine has to be refactored to expose the same operations as make_new_machine
-- empty constructor
-- all private fields come from make_new_machine

When the current instruction works:
- go to next instruction
